syntax = "proto3";

service Translation{
  rpc StreamMusic (MusicRequest) returns (stream AudioChunk);
}
message MusicRequest {
  string song_path = 1;
}

message AudioChunk {
  string data = 1;  // Данные аудио в формате байтов
}